import BlockAddForm from "./components/ObjectAddForm";
import { Object } from "./types/ojbect";
import { useState } from "react";

export default function BlockAdderApp() {
  return(
    const [objectConfig, setobjectConfig] =
      useState<ObjectConfig | null>(null);
    <BlockAddForm/>
  );
}